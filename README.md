Integrate the three major subdomain enumeration tools, subfinder, amass, and oneforall, and integrate the deduplication bash script. Please change the custom tool path and output file location when using it.

Use the command: ./d.sh domain.com -o result.txt
